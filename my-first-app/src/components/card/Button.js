export function ButtonBuy (url, text) {
return(
<a href={url} target ="_blank" rel="noreferrer">
    {text}
</a>
)
}
/*
export function ButtonDetails (details) {

    return(
    <button>{details}</button>
)
}

export default function Button(props) {
    const { url, text } = props;
    return (
      <a href={url} target="_blank" rel="noreferrer">
        {text}
      </a>
    );
  }
  */